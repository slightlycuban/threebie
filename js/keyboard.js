LEFT = 37;
UP = 38;
RIGHT = 39;
DOWN = 40;

function checkKey(e) {
  e = e || window.event;

  if (e.keycode == LEFT) {
    // rotate camera left
  }

  if (e.keycode == RIGHT) {
    // rotate camera right
  }
}
